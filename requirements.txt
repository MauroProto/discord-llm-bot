discord.py>=2.3.0
# LLM providers (only the one(s) you actually use need to be reachable at
# runtime — these are kept in `requirements.txt` for the multi-provider
# template so users can switch via LLM_PROVIDER without re-installing).
anthropic>=0.28.0
openai>=1.50.0
google-genai>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
aiohttp>=3.8.0
# Voz (la lib voice-recv solo publica pre-releases, requiere --pre al instalar)
# PyNaCl 1.6.2 patches CVE-2025-69277 but discord.py 2.7.1's voice extra
# pins PyNaCl<1.6,>=1.5.0, so we can't bump until discord.py releases a
# version that lifts that upper bound. The CVE involves memory handling
# that isn't exposed by a Discord bot's threat model (we only encrypt /
# decrypt audio frames coming from Discord's gateway, not user input).
# Tracked: revisit once discord.py >2.7.1 lands.
PyNaCl>=1.5.0,<1.6
discord-ext-voice-recv>=0.5.2a179
elevenlabs>=1.5.0
audioop-lts; python_version >= "3.13"
