discord.py>=2.3.0
# LLM providers (only the one(s) you actually use need to be reachable at
# runtime — these are kept in `requirements.txt` for the multi-provider
# template so users can switch via LLM_PROVIDER without re-installing).
anthropic>=0.28.0
openai>=1.50.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
aiohttp>=3.8.0
# Voz (la lib voice-recv solo publica pre-releases, requiere --pre al instalar)
PyNaCl>=1.5.0
discord-ext-voice-recv>=0.5.2a179
elevenlabs>=1.5.0
audioop-lts; python_version >= "3.13"
